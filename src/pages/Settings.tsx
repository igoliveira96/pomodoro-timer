import {Text, View} from 'react-native';

export const Settings = () => {
  return (
    <View>
      <Text style={{fontFamily: 'InterRegular'}}>
        Settings page
      </Text>
    </View>
  );
};
